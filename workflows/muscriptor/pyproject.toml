[project]
name = "polyscribe-workflow-muscriptor"
version = "0.1.0"
description = "MuScriptor full-mix transcription worker"
requires-python = ">=3.12,<3.13"
dependencies = [
    "muscriptor",
    "torchaudio",
]

[tool.uv]
package = false
